// Single flat price for the resize/convert unlock, regardless of tool or
// exam. Split this into a lookup keyed by tool/examCode later if specific
// exams warrant a different price.
module.exports = {
  unlockAmountPaise: 2900, // ₹29
};
